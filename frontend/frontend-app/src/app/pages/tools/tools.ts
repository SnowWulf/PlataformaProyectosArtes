import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-tools',
  standalone: true,
  templateUrl: './tools.html',
  styleUrl: './tools.scss',
  imports: [
    RouterLink
  ],
})
export class Tools {

}